[project]
name = "agonistes-langgraph"
version = "0.1.0"
description = "Project Agonistes — adversarial LLM debate layer (LangGraph + OpenRouter)"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.2",
    "openai>=1.30",
    "instructor>=1.3",
    "pydantic>=2.5",
]
